# Normalize all text to LF in the repo and on checkout. This keeps the committed
# generated source byte-identical across platforms so the CI drift check
# (git diff --exit-code on src/**/Generated) is stable.
* text=auto eol=lf

# Binary types that must never be normalized.
*.png binary
*.snk binary
*.nupkg binary
*.snupkg binary
