{
  "schema": {
    "type": "object",
    "required": ["customer"],
    "properties": { "customer": { "$ref": "#/$defs/Customer" } },
    "unevaluatedProperties": false,
    "$defs": {
      "Customer": {
        "type": "object",
        "required": ["email"],
        "properties": { "email": { "type": "string", "format": "email" } },
        "unevaluatedProperties": false
      }
    }
  },
  "value": { "customer": { "email": "a@example.com" } },
  "expect": { "valid": true }
}
